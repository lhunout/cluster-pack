pypandoc<1.8
pyspark
pytest<9.1  # pylama's pytest plugin uses the `path` hook arg removed in pytest 9.1
pyflakes==2.4.0
pylama
mypy
numpy<2  # breaking changes from numpy 2+, can be removed when all dependencies are compatible